import React from 'react';
import './App.css'

function Header({ OpenSidebar }) {
  return (
    <header className='header'>
       
        <h1 className="centered-text">Student Dashboard</h1>
    </header>
  )
}

export default Header;
